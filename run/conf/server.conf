# Adresse IP de l'interface (carte r�seau)
# Si l'adresse est 0.0.0.0 le syst�me choisira lui-m�me l'interface � utiliser
Interface = 0.0.0.0

# Num�ro du port TCP
Port = 8080

# Taille de la queue d'attente de la socket
Listen = 20

# Temps d'attente (en secondes) de r�ception d'une requ�te apr�s la connexion
Timeout = 5

# R�pertoire racine du site
DocumentRoot = .\web

# Fichier par d�faut
DirectoryIndex = index.html

# Fichier envoy� au client si une erreur se produit
#ErrorFile = nothing.htm

# Ficher contenant les correspondances entre les extensions de fichier et les types MIME
# Le chemin est relatif au r�pertoire de lancement du serveur
TypesConfig = conf\mime.conf

# Type MIME par d�faut
DefaultType = text/plain

